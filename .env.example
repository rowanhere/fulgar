# BrowserCoin miner configuration.
#
# Easiest path: just run `npm start` — it asks for your wallet, lets you pick a
# pool, and writes .env.local for you. Prefer to configure by hand? Copy this
# file to `.env.local` and fill it in. (.env.local is gitignored.)

# REQUIRED — your BrowserCoin wallet address (64 hex chars).
# Copy it from the app: Wallet → Copy. Mining rewards are paid to this address.
MINER_PUBKEY=

# OPTIONAL — where to mine. By default the miner routes to FulgurPool, the
# default pool (https://fulgurpool.xyz). You normally don't touch this.
#   (unset)           → FulgurPool (the default)
#   solo              → always mine on your own
#   https://your.pool → mine at a specific pool (https:// may be omitted)
# Pools are more easily added or removed from the Where to mine picker in
# `npm start` or `npm run settings` — or use pools.json for advanced setup.
# MINER_POOL=

# OPTIONAL — number of CPU worker threads. Default: (CPU cores - 1).
# MINER_WORKERS=4

# OPTIONAL — verifier threads used only while downloading/checking the chain.
# Default: up to 8, capped by MINER_WORKERS. Lower this if solo sync crashes with
# Node heap/OOM errors on a constrained VPS; mining still uses MINER_WORKERS.
# MINER_SYNC_WORKERS=4

# OPTIONAL — duty cycle 0.05–1.0: fraction of time spent hashing.
# Lower = cooler/quieter, higher = faster. Default: 0.75.
# MINER_THROTTLE=0.75

# OPTIONAL — comma-separated API helper URLs (used for solo mining / chain sync).
# Reads try each helper in turn and take the first that answers, so one dead helper
# doesn't stall the miner; it warns only if every helper fails a round. Blank or
# comma-only falls back to the defaults below. (A typo'd URL is NOT caught — it's
# used as written, and every read against it fails.)
# Default: https://api1.browsercoin.org,https://api2.browsercoin.org,https://api1.taitech.eu,https://api1.cryptec.tech
# MINER_HELPERS=https://api1.browsercoin.org,https://api2.browsercoin.org,https://api1.taitech.eu,https://api1.cryptec.tech

# OPTIONAL — set to 1 to use the native Rust engine (~parity with wasm on Sandglass).
# Same as choosing Engine -> native in the menu. If it isn't built yet, the app
# offers to build it on start (cargo build --release) when Rust is installed, and
# otherwise keeps using wasm — it never blocks. Build it by hand any time with:
#   cd native/brc-pow && cargo build --release
# MINER_NATIVE=1

# OPTIONAL — set to 1 to use the CUDA grinder for solo mining when built.
# Build with: cd native/brc-pow-cuda && make
# MINER_CUDA=1
# CUDA processes, default 1 per GPU. Do not set this to MINER_WORKERS on a
# many-core host: each CUDA process owns its own large scratch allocation.
# MINER_CUDA_WORKERS=1

# OPTIONAL — CUDA batch size per lane. Lower = more frequent hashrate updates;
# higher = fewer host wakeups. Default: 16.
# MINER_CUDA_NONCES_PER_LANE=16

# OPTIONAL — controls TUI mode (the full-screen dashboard). Default: auto-detect
# from the terminal (the dashboard is used when stdout is a TTY).
# Set to 0 to force plain logs even in a terminal.
# FULGUR_TUI=0

# OPTIONAL — pool mode only: how often (ms) to poll the pool for a fresh job.
# Default 1000. Lower picks up a newly-found block sooner (fewer stale shares);
# higher means fewer requests. Clamped to 250–60000. You normally don't touch this.
# JOB_POLL_MS=1000

# Pool /job long-poll: hold each /job request open until a new job rotates (or this
# many seconds elapse). Cuts the stale-job gap to ~milliseconds when the pool supports
# it. Backward-compatible: against a pool that ignores it, falls back to JOB_POLL_MS.
# Default 25, clamped 0..30; 0 disables long-poll.
# JOB_WAIT_S=25
